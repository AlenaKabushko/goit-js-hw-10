<div>
    <img src="{{flags.svg}}" alt="flag" width="50">
    <p>{{name.official}}</p>
</div>
    <p>Capital: {{capital}}</p>
    <p>Population: {{population}}</p>
    <p>Languages:{{#each languages}} {{ this }}, {{/each}}</p>

